{
  "name": "fill_me",
  "title": "Human-readable target name",
  "description": "What this coverage analysis is trying to measure.",
  "spec": {
    "profile": "fill_me",
    "source_of_truth": "Where the spec/scope decision comes from.",
    "included_extensions_or_features": [],
    "excluded_extensions_or_features": [],
    "notes": [
      "Keep target-specific spec assumptions here, not in SKILL.md or scripts."
    ]
  },
  "scope_in": [
    "List the architecture areas, Spike components, instruction classes, or behavior classes that belong in this target."
  ],
  "scope_out": [
    "List exclusions so scripts can filter obvious noise and the agent can avoid proposing out-of-scope tests."
  ],
  "summary_include_regex": [
    "^riscv/"
  ],
  "summary_exclude_prefixes": [],
  "summary_exclude_regex": [],
  "line_exclude_regex": [],
  "line_priority_regex": [
    "regex terms that should rank line-level functions higher for this target"
  ],
  "coverage_thresholds": {
    "low_line_pct": 20.0,
    "low_branch_pct": 10.0,
    "low_call_pct": 10.0
  },
  "source_priority": [
    "riscv/example.cc"
  ],
  "dimensions": {
    "example shared paths": [
      "riscv/mmu.cc",
      "riscv/mmu.h"
    ],
    "example instruction group": [
      "insn:lw",
      "insn:sw"
    ],
    "example glob group": [
      "glob:riscv/insns/amo*.h"
    ]
  },
  "analysis_notes": [
    "Optional notes for the agent about how to interpret coverage evidence for this target.",
    "Keep target-specific spec assumptions here."
  ],
  "path_analysis": {
    "purpose": "Optional path-aware evidence policy. Use this to describe how the agent should report and prove execution-flow coverage when gcov branch/call coverage is not enough.",
    "confidence_levels": [
      "confirmed-not-executed",
      "counter-increment-observed",
      "single-case-increment-confirmed",
      "edge-covered-path-unknown",
      "needs-path-instrumentation",
      "out-of-scope"
    ],
    "evidence_policy": [
      "Do not treat path_analysis as a complete path matrix or architecture specification.",
      "Derive path signatures from Spike source and gcov evidence; do not invent combinations from a predeclared Cartesian product.",
      "Use path_signature_fields only as a reporting checklist.",
      "If all relevant edges are covered only in aggregate, mark edge-covered-path-unknown.",
      "Use single-case increment evidence or per-instruction/access path markers before claiming same-flow path coverage."
    ],
    "path_signature_fields": [
      "target instruction or entry",
      "shared source function path",
      "must-pass source line/branch/call evidence",
      "source-proven condition under test",
      "architectural observable",
      "remaining uncertainty"
    ],
    "path_markers": {
      "marker.name": {
        "meaning": "What this marker proves when emitted by an instrumented Spike.",
        "suggested_locations": [
          "riscv/example.cc:function_or_branch"
        ]
      }
    },
    "single_case_increment": {
      "required_practice": [
        "Run one tiny single-purpose case after resetting or snapshotting coverage counters.",
        "Compare before/after .gcov snapshots for required line/branch/call counter increases.",
        "Use Spike -l --log-commits when useful to confirm the target guest instruction executed."
      ],
      "interpretation": [
        "If a required edge/call stays zero, the path is confirmed not executed.",
        "If required counters increase but isolated-case/log/profile evidence is incomplete, mark counter-increment-observed instead of single-case-increment-confirmed.",
        "If all required counters increase in a tiny single-purpose case and Spike log/profile evidence confirms the target instruction, mark single-case-increment-confirmed.",
        "If edges are covered only in aggregate, mark edge-covered-path-unknown."
      ]
    },
    "marker_correlation_policy": [
      "Strong marker evidence requires JSON per-instruction/access records with pc+insn, seq, or access_id correlation fields.",
      "JSON records without correlation fields and text fallback records are weak evidence only.",
      "Marker sequence observation is evidence; the agent must still validate marker placement and architectural observable."
    ]
  },
  "inspection_hints": {
    "example shared paths": [
      "example.cc.gcov",
      "example.h.gcov"
    ],
    "example instruction group": [
      "lw.h.gcov",
      "sw.h.gcov"
    ],
    "example glob group": [
      "amoadd_w.h.gcov",
      "amocas_w.h.gcov"
    ]
  },
  "duplicate_search_terms": {},
  "handoff_defaults": {
    "suggested_test_point_area": "",
    "suggested_case_area": "",
    "default_gate_note": "needs profile decision",
    "implementation_owner": "hyptest-workflow"
  },
  "special_run_scope": [
    "example shared paths"
  ],
  "manual_only_dimensions": [
    "example shared paths"
  ],
  "dimension_metadata": {
    "example shared paths": {
      "default_gate_allowed": false,
      "requires_runtime_option": true,
      "gate_note": "manual/special-run unless the selected platform makes this deterministic"
    }
  },
  "duplicate_search_aliases": {}
}
